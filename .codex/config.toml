# The repository intentionally has no root AGENTS.md. Reuse the shared agent
# protocol so Codex and Claude follow the same execution and logging rules.
project_doc_fallback_filenames = ["CLAUDE.md"]
project_doc_max_bytes = 32768
