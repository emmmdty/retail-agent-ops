# V6 迭代（V6-2/V6-2b）：v5 结构 + rtc_stepwise 请求 reason 修复（单变量）+ 解析器容忍。
# 预注册：task_plan.md Current Task（v6，commit 696ad81 + 修订 5810959，先于一切运行）；
# A-0 裁定（2026-09-07 用户逐项确认）：先诊断后定；dataset_version=retail_ops_v6_20260907；
# 判读 = v1.3 十二门 + 探针每点 ≥0.875、三分支完整划分；沿用 gate_schema 1.3；
# 缺闭合标签缺陷处置 = 解析器协议容忍（manifest 携带 hermes-single-call-v2-unterminated）。
#
# 复用 v4 bundle（工具/政策/12 类别序不变）；结构配额与 v5 完全相同（588/198/246），
# 唯一数据差异 = rtc_stepwise 请求 reason 与 gold 同源（PITFALLS #26 修复，版本键控）。
pipeline: formal_freeze
bundle_dir: domains/retail_ops/v4
dataset_version: retail_ops_v6_20260907
