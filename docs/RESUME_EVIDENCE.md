# 简历证据（R4 第三轮收口）

本文件是简历与面试表述的**唯一取数口径**。规则来自 `docs/CAREER_CONTEXT.md`：
只写实际运行得到的数字；不写 SOTA；不把内部固定子集说成官方榜单；
不把单次成功演示说成生产稳定性；**项目可以输出 NO-GO，能拒绝不合格候选本身是工程价值**。

**本版最重要的变化**：项目的核心结论不再是"某个候选达标"，而是一个 2×2 实验矩阵，
它把**提示词工程**与**后训练**的贡献分离到了类别级。这是绝大多数同类项目给不出的证据形态。

## 1. 可用数字与出处

每个数字都能追溯到产物、`report_id`/commit 与 LOG 条目。

### 1.1 流水线与资源

| 数字 | 值 | 出处 |
|---|---|---|
| 数据集规模 | train/dev/holdout = 240/60/120，六类均分，seed 0 | `manifests/retail_ops/v1/retail_ops_v1_r2_20260722/`，LOG-20260806-04 |
| teacher 采集通过率 | 238/240 = 99.2%（replay + 最终状态 + 政策 verifier 三重校验后） | LOG-20260806-12 |
| teacher 成本 | 240 条批次约 **$0.055**（519 次请求实测） | LOG-20260806-06 |
| QLoRA 训练（attention-only） | 单卡 3 epoch / 75 steps，**212 s**，峰值 **5.55 GB**，adapter **23.6 MB** | `reports/…/r4/sft-004/metrics.json` |
| QLoRA 训练（全 linear layer） | 同超参，**223 s**，峰值 **5.64 GB**，adapter **66 MB** | `reports/…/r4/sft-003/metrics.json` |
| 推理资源 | 4-bit NF4，dev 评测峰值显存 **2.95–3.04 GB** | 各 `candidate-report.json` 的 `hardware.gpu.peak_memory_bytes` |
| 工程基线 | **688** tests passed；Ruff / mypy(65 源文件) / `uv lock --check` / `git diff --check` 全绿 | 每次收口均重跑 |
| 环境缺陷修复 | `refund_denied_window` 类通过率 **30% → 95%**（暴露 `current_day` 后） | LOG-20260806-07 |

### 1.2 核心结果：prompt × 容量的 2×2 矩阵（**dev，60 条**）

| | 零训练 | attention-only 训练 | **全 linear layer 训练** |
|---|---|---|---|
| **原 prompt** | 48/60 | 45/60 | **60/60** |
| **新 prompt**（+1 句授权自主执行） | 54/60 | 55/60 | **60/60** |

出处：`base-001` / `base-002` / `sft-002` / `sft-005` / `sft-003` / `sft-006` 的
`candidate-report.json` 与私有 `trajectories.jsonl`；LOG-20260814-01/02/03。

### 1.3 类别级因果分解（**这是最有解释力的一张表**）

| 贡献来源 | refund_eligible | refund_recovery | policy_violation | invalid_call |
|---|---|---|---|---|
| 基座（原 prompt，零训练） | 5/10 | 5/10 | 8 | 21 |
| **+ 提示词**（零训练） | **9/10** (+4) | 5/10（**0 变化**） | 5 | **0** |
| **+ 后训练**（全 linear） | 10/10 (+1) | **10/10** (+5) | **0** | 0 |

**一句话**：提示词能解除「判定可退却不敢执行」，但教不会「失败后重试」；
后者只能来自轨迹数据。两者修的是不同的失败，几乎不重叠。

### 1.4 被自己实验推翻的三个判断

| 判断 | 推翻方式 | 出处 |
|---|---|---|
| 根因是训练数据的动作分布失衡 | 单变量实验把关键决策点比例 3:1 → 1:1，目标类别通过数变化**精确为 0** | LOG-20260811-09 |
| 改 `SYSTEM_PROMPT` 后重新导出即可换掉训练集的 system 消息 | 实测 238/240 条逐字节不变（teacher 证据持久化了 metadata），且不报错——会静默产出变量没生效的数据集 | LOG-20260814-01 |
| 候选 A 的 dev +0.200 是训练挣来的 | 该 delta 对照的是原 prompt 的基座；同 prompt 两侧重新配对后训练的独立增量是 **+0.100** | LOG-20260814-03 |

### 1.5 封存 holdout（**第一次观测，R3 候选**）

| 数字 | 值 | 出处 |
|---|---|---|
| 基座 | task_success **0.7833（94/120）**，政策违规 16，非法调用 41，schema 0.7819 | `report_id=b538a6c4…` |
| R3 候选 | task_success **0.7500（90/120）**，政策违规 **0**，非法调用 **0**，schema **1.0000** | `report_id=a8cfcf38…` |
| 发布判定 | **NO-GO / deployment=baseline**，唯一失败门禁 `success_delta`（−0.0333 < +0.05） | `formal-release-001/release.json`，LOG-20260811-03 |
| 服务 | 按 NO-GO 回滚加载纯基座，允许/拒绝/异常恢复三条流程成功，并发超限返回 503 | LOG-20260811-04 |

**第二次观测状态**：配置与守卫已就绪并经突变验证（`tests/test_retail_ops_r4_release_configs.py`），
**尚未执行**。因此本文件目前**没有任何"改进后 holdout 提升"的数字可写**。

## 2. 明确不可写的表述

| 不可写 | 原因 |
|---|---|
| "任务成功率提升 20pp" | 那是 **dev** 且对照原 prompt 基座。同 prompt 配对后训练的独立增量是 +0.100；holdout 未测 |
| "候选在 holdout 上优于基座" | 第一次观测是**下降** 3.3pp 判 NO-GO；改进后的第二次观测**还没跑** |
| "dev 60/60 证明模型能力" | 每类 n=10，且 **dev 已被用于从 4 个候选中选出这一个**，存在选择偏差，holdout 必然回落 |
| "CI95 显示显著改善" | 60/60 的 `task_success_ci95` 是 [1.0,1.0]，那是满分的退化区间，不是显著性证据 |
| "达到 SOTA / 超过 GPT-x" | 无跨模型对比，内部固定子集不是公开榜单 |
| "生产可用 / 稳定服务" | 单次演示，无压测、无长稳、无多租户 |
| "prompt 工程无用" 或 "SFT 无用" | 两者都被数据否定：prompt 独占 `refund_eligible` 的 +4，训练独占 `refund_recovery` 的 +5 |
| "延迟数可跨运行比较" | 共享 GPU，各次运行他人占用 0%–98% 不等，`train_runtime` 223 s 与 294 s 之差属占用差异 |
| "泛化到多领域" | 2 工具 / 6 类 / 单一中文零售退款场景；跨模型规模未验证 |
| "定位根因为训练数据动作长度失衡" | **该假设已被自己的实验证伪**（§1.4 第一行） |
| BFCL 相关成绩 | 属 legacy 轨道，与主线无关，163/200 与 167/200 只能表述为项目固定单轮 AST 子集结果且不得声称稳定提升 |

## 3. 简历 bullet：两个方案（**需你选定**）

两版都只用 §1 的数字。**我推荐方案 B**——§1.2/§1.3 那两张表是这个项目现在最难被复制的部分，
而它们天然属于后训练/算法叙事。

### 方案 A —— 面向 Agent 平台 / Agent Evaluation / 系统开发岗

> **RetailAgentOps｜零售工具 Agent 的单卡适配与发布流水线**（个人项目，Python/PyTorch/FastAPI）
> - 设计并实现 `build / evaluate / release / serve` 四接口流水线，覆盖轨迹数据质检、
>   冻结任务评测、版本化发布门禁与单卡服务；**688** 项测试、Ruff/mypy/依赖锁全绿。
> - 建立**不可伪造的证据链**：运行报告以全字段自哈希为 ID、逐产物 SHA-256 绑定、
>   配对比较前强制校验模型/生成参数/数据/commit 同条件，任一字段被改动即加载失败。
> - 实现封存 holdout 的两段式授权与五维指纹隔离，公开产物为 allowlist 字段集并有
>   泄漏扫描测试；120 条 holdout 在整个开发期**仅被观测一次**，用于发布判定。
> - 用真实候选跑通发布决策：候选将政策违规 **16→0**、非法调用 **41→0**、
>   schema 有效率 **0.78→1.00**，但任务成功率未达 +5pp 门槛，系统判 **NO-GO** 并驱动
>   服务回滚到冻结基座（`adapter_loaded=false` 可在 `/health` 核对）。
> - 把评测系统用于**因果归因**而非仅打分：以 2×2 配对实验分离提示词与后训练的贡献，
>   在类别级证明两者修的是不同失败（提示词 +4、训练 +5，几乎不重叠）。

**主语是系统与证据。** 适合"我们需要有人把 Agent 的评测与上线流程做扎实"的岗位。

### 方案 B —— 面向 Agent 后训练 / 算法工程岗（**推荐**）

> **RetailAgentOps｜零售工具 Agent 的领域适配与失败归因**（个人项目，Python/PyTorch/PEFT）
> - 构建执行式训练数据流水线：商业 API teacher 采集 + replay/最终状态/政策 verifier
>   三重校验，240 条训练轨迹通过率 **99.2%**，成本 **$0.055**；train/dev/holdout
>   五维指纹隔离防派生泄漏。
> - 单卡完成 Qwen3-4B QLoRA-SFT（4-bit NF4，r=16，3 epoch，**223 s**，峰值 **5.64 GB**，
>   adapter **66 MB**），评测推理峰值显存 **3.0 GB**。
> - **以 2×2 配对实验分离提示词工程与后训练的贡献**（prompt × LoRA 覆盖范围，
>   六次运行、每格 60 条冻结任务）：一句显式授权指令使零训练基座的目标类别
>   **5/10 → 9/10**，而"失败后重试"类**零变化**；后训练则把重试类 **5/10 → 10/10**、
>   政策违规 **5 → 0**。两者修的是不同失败，几乎不重叠。
> - **发现 LoRA 覆盖范围决定训练效果的符号**：仅挂 attention 投影时，SFT 对目标类别是
>   **净负作用**（基座 5/10 被训练降到 0/10，跨两个提示词、四次独立训练方向一致）；
>   加上 MLP 三投影后，**同一份数据、同一组超参**从负作用变为 **10/10**。
> - 每轮改动前写定收益门槛并按门槛判负，全程被自己的实验推翻三个判断（含"根因是数据
>   分布失衡"与"这个 delta 是训练挣来的"），据此确立"最终状态与政策 verifier 是唯一
>   主判据，loss/奖励不可代替"——`verifier_reward` 已四次与主判据背离。

**主语是模型与归因。** 适合"我们需要有人把领域模型调出来并解释清楚为什么"的岗位。

### 共用的一句话总结（自我介绍开场可用）

> 我做了一条零售工具 Agent 的单卡适配与发布流水线。最有意思的结论不是模型变好了多少，
> 而是我用一组配对实验把"提示词工程"和"后训练"的功劳分开了——一句提示词就解决了
> 大半的"不敢执行"问题，而"失败后重试"只能靠训练；并且发现 LoRA 只挂 attention 时，
> 微调对这个能力其实是**负作用**。这套结论是我自己的实验推翻了我自己前两轮的判断得到的。

## 4. 面试主动交代的四件事

不等对方问，主动说，可信度更高（细节见 `DEMO.md` §4）：

1. **dev 上的 60/60 带选择偏差** —— 它是从四个候选里选出来的，每类只有 10 条，
   holdout 上必然回落。改进后的 holdout 判定尚未执行，所以我现在**没有**"提升"可以报。
2. **演示挑的三条成功轨迹不代表能力** —— 同批次另一条 `refund_eligible` 仍然失败，
   基座在 holdout 上是 94/120。
3. **有一个环境缺陷是真实 API 采集才暴露的** —— `refund_denied_window` 场景的判定依据
   `current_day` 从未暴露给模型，任何推理式 Agent 都无解；CPU 测试全走 Oracle 所以没发现。
   真实采集通过率 30% 才暴露，修复后 95%。
4. **我的判断被自己的实验推翻了三次**（§1.4）—— 最值得讲的是第三次：我一度可以用
   "候选比基座高 20pp"来写简历，是我自己去测了"给基座也换上那句提示词"，才发现那 20pp
   里有一半是免费的、跟训练无关。**如果不做那次对照，这个数字放到简历上就是虚的**，
   而面试官只要问一句"你的基座用什么提示词"就会露。

## 5. 尚不可用、需后续阶段补齐

| 缺口 | 状态 |
|---|---|
| **改进后候选的 holdout 判定** | 配置与守卫已就绪并经突变验证，**尚未执行**；第二次观测未消耗。这是当前最高优先级 |
| 跨模型规模验证（Qwen3-1.7B 上复现"容量决定符号"） | 未做。1.7B 基座已有（42/60），成本约 20 分钟 GPU，可把结论从单模型观察升级为跨规模规律 |
| A+B 叠加（终局回复 + 容量） | 未测，**不得推断** |
| 故障测试（超时、幂等、策略冲突、资源限制、回滚故障） | R5 |
| 双语 README、架构图、演示视频 | R5 |
| 独立重建复验（若最终要写"提升"，必须先做这一次） | holdout 判定之后 |
| 跨 benchmark（τ²-bench 等） | 评估为低优先级：边际求职价值低于上面各项 |
