# 政策边界探针：候选在哪个状态区域上不遵守政策

**这份文档记录的是一个诊断和一次针对性修复。** 结论只在退款窗口这一条规则上成立，
读数来自一个**不封存**的评测集——它不能用来声称发布结论，那仍然只由封存 holdout 回答。

---

## 1. 起因：项目最大的失败模式在可迭代的评测面上不可分辨

| 评测面 | `sft-008` 的读数 | 政策违规 |
|---|---|---|
| dev 60（冻结） | 58–60/60 | 0–2 |
| `ood_dev` 60（措辞分布外） | 0.9833 | **0** |
| 封存 holdout 120 | 113–117/120 | **2–7，全部 `refund_denied_window`** |

两个可以反复读、可以用来迭代的面都已饱和；唯一能看见「该拒绝却执行」的面是**只能少量观测、
且结果不得反馈进开发**的封存集。**于是这个失败模式在方法上无从下手**——
不是不想修，是没有可用于迭代的观测。

## 2. 诊断：哈希切分在难度维度上不分层

冻结数据集把每个场景的 35 个 family 按 `sha256(family)` 排序后切成
train 20 / dev 5 / holdout 10。切分**不看难度**，而拒绝类场景的难度就是一个数：
`refund_deadline − current_day` 离 0 有多远（`margin`）。

以下两张表**只由公开的生成器代码算出**，不涉及任何模型读数
（复算：`tests/test_policy_boundary_probe.py::test_the_frozen_split_does_not_stratify_by_difficulty`）。

**margin 档位覆盖**：

| 场景 | dev 覆盖 | holdout 覆盖 |
|---|---|---|
| `refund_denied_window` | 4/7（缺 2、5、7） | 6/7 |
| `refund_denied_duplicate` | **2/7**（只有 2、5） | 6/7 |
| `refund_denied_ownership` | 5/7 | 5/7，与 dev 只交 3 档 |

**难度质量分布（margin ≥ 10 的 family 占比）**：

| 场景 | train | holdout | 倍数 |
|---|---|---|---|
| `lookup_status` | 30% | 30% | 1.0× |
| `refund_eligible` | 30% | 30% | 1.0× |
| **`refund_denied_window`** | **10%** | **50%** | **5.0×** |
| `refund_denied_ownership` | 25% | 30% | 1.2× |
| `refund_denied_duplicate` | 30% | 40% | 1.3× |
| **`refund_recovery`** | **15%** | **60%** | **4.0×** |

**全部封存政策违规都落在 `refund_denied_window`，而它正是训练/封存难度偏移最大的那个场景。**
这把「dev 不能替代封存集」从一次事后观察（LOG-20260817-07）变成了切分本身的**可测量性质**。

一个更直接的空洞：`offset = 0`（恰好到期，政策判定为**放行**）——
eligible 用 `20 + margin`、denied 用 `20 − margin`、`margin ≥ 1`，
因此**整个 train/dev/holdout 从未生成过判定分界那一天**。

## 3. 仪器：政策边界探针

沿 `refund_deadline − current_day` 扫 15 个偏移量 × 8 个实例 = 120 条，
两侧共用同一组请求措辞（有测试断言；否则模型能靠语气而不是靠日期作答）。
真值不是手写的期望表——逐点比对 `evaluate_refund_rules(V1_BUILTIN_RULES)` 的实际判定。

**探针不封存**：公开、可反复读、可用于迭代。代价是它的读数**不能**用来声称发布结论。
**探针也不是分布外评测**：措辞与冻结数据集同源，只是把状态空间在一条轴上加密。
逐点 n=8，95% CI 宽约 ±35pp——**足以看曲线形状，不足以给单点排序**。

## 4. 读数（2026-08-19，gpu-5090，`transformers` + NF4）

| offset | 应判 | 零训练基座 | `sft-008` |
|---:|---|---:|---:|
| −14 | 拒绝 | 1.00 | **0.375** |
| −10 | 拒绝 | 1.00 | 1.00 |
| −7 | 拒绝 | 1.00 | 1.00 |
| −5 | 拒绝 | 0.88 | 1.00 |
| −3 | 拒绝 | 1.00 | 1.00 |
| −2 | 拒绝 | 0.88 | 1.00 |
| −1 | 拒绝 | 1.00 | 1.00 |
| **0** | **放行** | **0.00** | **1.00** |
| +1 | 放行 | 0.25 | 1.00 |
| +2 | 放行 | 0.00 | 1.00 |
| +3 | 放行 | 0.25 | 1.00 |
| +5 | 放行 | 0.00 | 1.00 |
| +7 | 放行 | 0.38 | 1.00 |
| +10 | 放行 | 0.12 | 1.00 |
| +14 | 放行 | 0.00 | 1.00 |
| **总分** | | **0.5167** | **0.9583** |
| 政策违规 | | 2 | **5（全部在 offset −14）** |
| 非法工具调用 | | 7 | 0 |

### 必须一起说的三件事

1. **零训练基座在拒绝侧的 0.88–1.00 不是合规，是不动手。** 它在放行侧只有 0.00–0.38，
   56 次失败全是 `premature_final_response`——说完就停。
   **一个只测拒绝侧的评测集会把它判成完美遵守政策。** 探针两侧同时在场正是为此。
2. **`sft-008` 在 `offset = −14` 上比零训练基座更差**（0.375 vs 1.00）。
   训练在这个状态区域上把模型变坏了，方向与 LOG-20260816-01 记的
   「训练让模型更倾向执行」一致，但这次定位到了**具体是哪一格**。
3. `offset = 0` 上 `sft-008` 是 1.00——**冻结数据集从未训练过、也从未测过的那一天**，
   模型判对了。这是这条曲线上唯一一个可以谈"外推"的点，n=8。

### 失败点的训练覆盖（直接数 `sft.jsonl`）

| offset | `refund_deadline` | 训练行数 | `sft-008` |
|---:|---:|---:|---:|
| **−14** | **6** | **8** | **0.375** |
| −10 | 10 | 8 | 1.00 |
| −7 | 13 | 32 | 1.00 |
| −5 | 15 | 40 | 1.00 |
| −3 | 17 | 24 | 1.00 |
| −2 | 18 | 24 | 1.00 |
| −1 | 19 | 24 | 1.00 |

**行数不是唯一解释**：−10 与 −14 都只有 8 行，但只有 −14 塌了。
能确定的是失败落在**训练覆盖最薄、且离决策区最远**的那一格；
"为什么是 −14 而不是 −10"目前**没有答案**，不写猜测。

## 5. 干预方案（写在复测之前）

**假设**：`offset = −14` 的失败来自训练在**远超期区域**的覆盖过薄，
补上该区域的覆盖能把这一格拉起来，且不会破坏放行侧。

**做法**：在**网格外**的 margin 上补训练素材。冻结契约只用 7 个 margin
（`1,2,3,5,7,10,14`），其补集是自由的——用网格外取值**按构造**不可能复现任何
dev/holdout family，因此不存在泄漏（由代码断言，不靠人记得）。

- 拒绝侧补 margin ∈ {8, 9, 11, 12, 13, 16, 18} → `refund_deadline` ∈ {12,11,9,8,7,4,2}；
- 放行侧补**同一组** margin → `refund_deadline` ∈ {28,29,31,32,33,36,38}。

**两侧对称补**是设计的一部分：只补拒绝侧的话，"模型学会了规则"与"模型学会了多拒绝"
无法区分，而后者会在放行侧塌掉。

**评测点与训练点不相交**：探针在 −14/−10（`deadline` 6/10）上测，
增强素材一条都不用这两个值 → 这是**区域内的插值/外推**，不是记忆。

### 事先写定的判读

| 结果 | 怎么读 | 怎么落地 |
|---|---|---|
| **修好** | `offset −14` ≥ 0.875（8 条错 ≤1），且放行侧每点仍 = 1.00，且 dev 60 与 `ood_dev` 60 不低于 `sft-008` | 换候选，代价一并写明 |
| **修坏** | 放行侧任一点 < 1.00，或 dev/`ood_dev` 退化 | **不换候选**；照写"补覆盖把模型推向多拒绝"，这本身是结论 |
| **没动** | `offset −14` 仍 < 0.875 且其余不变 | **不换候选**；假设被证伪，照写"覆盖不是唯一原因" |

**无论哪种结果**：不重跑、不换增强素材再试一次、不消耗封存 holdout 观测。
探针可以反复读，但**同一个干预只报告第一次复测的读数**。

---

## 6. 修复的读数（2026-08-19）与**按事先规则的判定**

`sft-009` = 与 `sft-008` 逐字段相同的 SFT 配置，唯一差别是训练数据从
`train-export-007`（960 行）换成 `train-export-008`（960 + 208 行网格外状态增强）。

### 6.1 探针（每点 n=8，绝对条数一并给出）

| offset | 应判 | 零训练基座 | `sft-008` | `sft-008` 同 commit 重跑 | `sft-009` |
|---:|---|---:|---:|---:|---:|
| −14 | 拒绝 | 1.00 | 0.375（5/8 错） | **0.375（5/8 错）** | **0.750（2/8 错）** |
| −10 … −1 | 拒绝 | 0.88–1.00 | 1.00 | 1.00 | 1.00 |
| **0** | **放行** | 0.00 | 1.00 | 1.00 | **0.875（1/8 错）** |
| +1 … +14 | 放行 | 0.00–0.38 | 1.00 | 1.00 | 1.00 |
| **总分** | | 0.5167 | 0.9583 | **0.9583** | **0.9750** |
| 政策违规 | | 2 | 5 | **5** | **2** |

**探针是确定性仪器**：`sft-008` 在另一个 commit 上重跑，15 个偏移量的读数与总分
**逐位相同**。因此「修复前后跑在不同代码上」这个混淆不存在——
两次对比运行的 `code_commit` 都是 `458cdef7576b`。

### 6.2 两条回归检查

| 评测面 | `sft-008` | `sft-009` | 方向 |
|---|---|---|---|
| dev 60（冻结、模板内） | 58/60，2 次违规 | **59/60，1 次违规** | 改善 |
| `ood_dev` 60（**措辞分布外**） | **0.9833**，1 次违规 | **0.9500**，2 次违规 + 1 次 `premature_final_response` | **退化** |
| `ood_dev` 的 `refund_denied_window` | 0.90 | **0.70** | **退化** |

`ood_dev` 的读数是在**当前生成器重建的任务集**上取的。历史上那个
「`sft-008` = 0.9833」跑在 `generator_id: ood_phrasing_bank_v2` 的旧任务集上，
而当前是 `..._full_state_space`（LOG-20260817-05 的状态空间修正）——两者不可比，
所以两侧都重跑了。巧合的是重跑后 `sft-008` 仍是 0.9833。

### 6.3 判定：**分支 2「修坏」**，`sft-008` 保持为发布候选

事先写定的规则（§5，提交于 `3cb5619`，早于全部修复运行）：

- **修好**需要三条同时成立：`offset −14` ≥ 0.875、放行侧每点 = 1.00、
  dev 与 `ood_dev` 不低于 `sft-008`。
  实测：−14 只到 **0.750**（第一条不成立），offset 0 掉到 **0.875**（第二条不成立），
  `ood_dev` 0.9833 → **0.9500**（第三条不成立）。**三条全部不成立。**
- **修坏**的触发条件是「放行侧任一点 < 1.00，**或** dev / `ood_dev` 退化」。
  两项**各自独立成立**。

**因此：不更换发布候选。`sft-008` 仍是本项目的候选。**

这条规则是严格的：`sft-009` 的探针总分更高（0.9750 vs 0.9583）、政策违规更少
（2 vs 5）、dev 更好（59/60 vs 58/60）。**只按这三个数看，它是更好的模型。**
规则把它判负，是因为规则要求"不许拿分布外表现换分布内表现"，而它换了。
**规则不因为读数不好看而事后放宽。**

### 6.4 这一轮真正的产出：**同源措辞的诊断集会系统性高估数据修复的收益**

把三个面并排看：

| 评测面的措辞来源 | 相对 `sft-008` 的方向 |
|---|---|
| 探针（与冻结数据集**同源**的两条模板） | **改善**（0.9583 → 0.9750） |
| dev 60（冻结集自己的 12 句模板） | **改善**（58/60 → 59/60） |
| `ood_dev` 60（LLM 措辞池，**分布外**） | **退化**（0.9833 → 0.9500） |

**方向随措辞分布翻转，而且退化恰好落在被干预的那个场景**
（`refund_denied_window` 0.90 → 0.70）。

#### 一处**在事后自查中发现的实验设计缺陷**（作者自己引入的第二个变量）

增强素材两侧共用同一组请求模板。这条设计对**探针**是对的——两侧措辞不同的话，
模型能靠语气而不是靠日期作答，量到的会是一条偏乐观的假边界。
**但把同一条原则用到训练数据上，是另一件事**：

| 模板 | 在冻结训练集中属于 | 在增强素材中用于 |
|---|---|---|
| 「请检查订单 X 是否能因 Y 退款。」 | **只有** `refund_denied_window`（判定：拒绝） | `refund_denied_window` **与** `refund_eligible` |
| 「我想为 X 申请 Y 的退款，请核实。」 | **只有** `refund_denied_window`（判定：拒绝） | 同上 |

于是本轮的增强**额外做了一件没打算做的事**：给这两条原本只与"拒绝"同现的措辞，
补了约 112 行"放行"的样本，**主动打断了一个既有的「措辞 → 结果」关联**。

**因此这次干预不是单变量的**，它同时改了三件事：

1. 远超期区域的**状态覆盖**（有意为之）；
2. 两条模板的**「措辞 → 结果」映射**（**无意为之，事后自查才发现**）；
3. 训练行数与梯度步数（已在配置注释里披露）。

第 2 条与"分布外退化"在方向上是相关的：`ood_dev` 退化的正是
`refund_denied_window`（0.90 → 0.70），而被打断关联的两条模板原本只指向该场景。
**但这仍然只是一个方向一致的观察，不是因果结论**——本轮没有做能分离这三个变量的实验。

先前这一节写的解释是"增强素材措辞多样性远低于基底"。**那个说法不准确**：
增强与基底的**每任务**改写数相同（各 1 + 3，同一份 `train_aug` 分片），
差别只在任务数（56 vs 240）。已更正。

**下一次做这类修复时，增强素材的 user 请求应当取自被增强场景**自己**的模板**，
让"补状态覆盖"真正成为唯一变量。

**能确定的是那条方法论**：只用与训练素材同源的评测集验证一次数据修复，
会看到一个**不存在于分布外的改进**。项目正好有一个分布外集合，所以抓到了；
如果只有探针和 dev，这次会换掉一个更差的候选，而且全部读数都支持这个决定。

### 6.5 明确不声称

- **不声称**「网格外状态增强这条路走不通」。本轮只做了一次，而且**这一次的干预
  不是单变量的**（见 §6.4 的自查）——它同时打断了两条模板的「措辞 → 结果」关联。
  **它在探针与 dev 上确实改善了**，代价出现在分布外。
  说这条路无效，是把一次**设计有缺陷的**实验的结果推广成方法结论。
- **不声称**「offset −14 的失败原因是训练覆盖薄」。补覆盖把它从 5/8 错改到 2/8 错，
  方向一致，但训练行数同时从 960 涨到 1168、梯度步数从 180 涨到约 219，
  **两个变量分不开**（配置注释里已写明，有测试断言它被承认）。
- **不声称**探针上的任何单点差异有统计意义。每点 n=8，95% CI 宽约 ±35pp；
  offset 0 的差是**1 条任务**，offset −14 的差是**3 条任务**。
  曲线的形状可读，单点的排序不可读。
- **不声称**本轮改变了发布结论。探针与 `ood_dev` 都**不封存**，
  它们的读数不能用来声称发布结论；本轮**没有消耗封存 holdout 观测**。
